<%= simple_form_for @tipomenu, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nombre %>
  <%= error_span(@tipomenu[:nombre]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tipomenus_path, :class => 'btn btn-default' %>
<% end %>
